
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ)

	Если ОбменДанными.Загрузка тогда
		Возврат;
	КонецЕсли;
		
	Сообщение = новый СообщениеПользователю();
	//Сообщение.Текст = СтрШаблон("Справочник %1 записан!", Наименование);
	Сообщение.Текст = СтрШаблон("Справочник %1 записан по новой!", Наименование);
	Сообщение.Сообщить();

КонецПроцедуры
